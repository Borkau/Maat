The app is under active development and is NOT working yet.

Test.
Web Maven MVC application with CRUD operations.
Data in Mysql is loaded from .xml files.
Technologies used:
- Java 14;
- Maven;
- Spring Boot;
- Spring Web;
- Spring Data JPA;
- MySQL;
- Lombok lib

Тестовое задание.
Веб-приложение, MVC, построенное на Maven, реализующее стандартные CRUD-операции.
Данные для MySQL парсятся непосредственно в приложении.

//////////////////////////////////////////////////////
* create MySQL tables

CREATE TABLE `moex_db`.`securities` (
  `id` INT NOT NULL,
  `secid` VARCHAR(45) NOT NULL,
  `shortname` VARCHAR(45) NOT NULL,
  `regnumber` VARCHAR(45) NULL,
  `secname` VARCHAR(100) NOT NULL,
  `isin` VARCHAR(45) NULL,
  `is_traded` INT NULL,
  `emitent_id` INT NULL,
  `emitent_title` VARCHAR(100) NULL,
  `emitent_inn` BIGINT(10) NULL,
  `emitent_okpo` INT NULL,
  `gosreg` VARCHAR(45) NULL,
  `sectype` VARCHAR(45) NULL,
  `secgroup` VARCHAR(45) NULL,
  `primary_boardid` VARCHAR(45) NULL,
  `marketprice_boardid` VARCHAR(45) NULL,
  PRIMARY KEY (`id`),
  UNIQUE INDEX `id_UNIQUE` (`id` ASC) VISIBLE);


CREATE TABLE `moex_db`.`histories` (
  `ID` INT NOT NULL AUTO_INCREMENT,
  `BOARDID` VARCHAR(45) NOT NULL,
  `TRADEDATE` VARCHAR(45) NOT NULL,
  `SHORTNAME` VARCHAR(45) NULL,
  `SECID` VARCHAR(100) NOT NULL,
  `NUMTRADES` DOUBLE(45) NULL,
  `HISVALUE` DOUBLE NULL,
  `HISOPEN` INT NULL,
  `LOW` DOUBLE(100) NULL,
  `HIGH` DOUBLE(10) NULL,
  `LEGALCLOSEPRICE` DOUBLE NULL,
  `WAPRICE` DOUBLE(45) NULL,
  `HISCLOSE` DOUBLE(45) NULL,
  `VOLUME` DOUBLE(45) NULL,
  `MARKETPRICE2` DOUBLE(45) NULL,
  `MARKETPRICE3` DOUBLE(45) NULL,
  `ADMITTEDQUOTE` DOUBLE(45) NULL,
  `MARKETPRICE3TRADESVALUE` DOUBLE(45) NULL,
  `ADMITTEDVALUE` DOUBLE(45) NULL,  
  PRIMARY KEY (`id`),
  UNIQUE INDEX `id_UNIQUE` (`id` ASC) VISIBLE);

//////////////////////////////////////////////////////
